rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Sadece Google ile giriş yapmış kullanıcılar okuyup yazabilir
    // Tarih sınırı yok — süresiz çalışır
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
